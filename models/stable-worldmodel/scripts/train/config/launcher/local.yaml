# @package _global_
# Local launcher configuration (no SLURM)

defaults:
  - override /hydra/launcher: basic

n_gpus: 1
cpus_per_task: 8
cache_dir: null # Use stable-worldmodel default cache
wandb:
  enabled: True
  config:
    entity: lewm_3DGeo
    project: DINO-WM
    name: overfit_1_sample
    id: ${subdir}
    resume: allow
    log_model: False
